# Reports List — Modul 18 Dashboard

**Kelompok A.** Satu respons agregat 20 kunci teratas (dokumen lama menyebut 21 —
cek! `dashboard.service.ts:106–127`) yang dirender sebagai 8 seksi halaman
(tanpa file/unduhan — "laporan" di sini = kartu di layar!). Konfirmasi cakupan:
hanya 1 endpoint (`POST dashboard/summary`, `dashboard.controller.ts:15–20`) +
1 halaman (`/dashboard`, `module-registry.tsx:200–207`); grep `apps/web/src` hanya
satu pemanggil `dashboard/summary` (`dashboard-page.tsx:217`).

| # | Seksi (judul + deskripsi persis!) | Sumber field | Logika + navigasi |
|---|---|---|---|
| 1 | 4 KPI: `Order Perlu Aksi` (`{p} pending, {a} aktif`) · `Penjualan Net Bulan Ini` (`{n} order selesai pada {Bulan Tahun}`) · `Piutang Terbuka` (`Sudah memperhitungkan pembayaran, alokasi, dan retur`) · `Stok Kritis` (`Total stok aktif cabang dibandingkan minimum produk`) | `order_summary.pending+active`, `month_sales_amount(+month_sales_order_count)`, `total_receivable`, `critical_stock_count` | Pending+active, net-sebulan (`money()`), piutang-net (`money()`), kritis-produk; nada warn/info/warn/bad; klik → `/orders?status_group=pending`, `?status_group=completed`, `/finance/receivables-payables`, `/stock?critical_only=true` (`dashboard-page.tsx:374–403`) |
| 2 | Strip hari: `Penjualan Net Hari Ini` (`{n} pesanan tercatat hari ini.`) | `today_sales_amount(money)`, `today_order_count` | Net-hari ini + hitung-semua-jenis (tanpa-status ikut hitung!); `formatCurrency` 0-desimal |
| 3 | Strip utang: `Utang Supplier Terbuka` (`Saldo pembelian yang belum dilunasi.`) | `total_payable(money)` | Utang-net (jepit-per-baris `GREATEST(…,0)`!); merah bila >0 |
| 4 | Strip pantau: `Produk Stok Dipantau` (`Produk aktif yang memakai kontrol stok.`) · `Kanal WhatsApp` (`{n} SOP siap dipakai.`) | `tracked_product_count` (perusahaan!), `whatsapp_status{state,phone,updated_at}`, `knowledge_ready_count ?? knowledge_document_count` | Produk-aktif-lacak, WA (`Terhubung` bila `connected/ready`, `Menghubungkan…` bila `connecting`, else `Belum terhubung`), SOP-siap (siap-else-total!) |
| 5 | Tren 6 bulan: `Tren Penjualan Net` (`Nilai penjualan selesai setelah koreksi retur dan tukar barang.`) | `sales_trend[{month:YYYY-MM, total_sales:money, order_count}]` | Bucket net + hitung-order-tanpa-retur (retur=0!); kosong=0; label `Mon yyyy id-ID`; tooltip `Penjualan Net`; bar 40/56px radius-6; fallback lokal tanpa-retur |
| 6 | 5 Terlaris: `5 Barang Paling Laku Bulan Ini` (`Periode {bulan}.`) | `top_sold_products_month[{id_product, product_code, product_name, base_uom, net_quantity_in_base_uom, net_revenue_amount:money, transaction_count}]` | Qty-net + omzet-tanpa-pajak (`line_total_before_tax else line_total−pajak`); retur-net (returned −, replacement +); ambang `qty>0.0001 ATAU Rp>0.009`; urut qty-turun; baris `{kode|Tanpa kode} \| {n} transaksi`; klik → `/products/{id}`; kosong → info `Belum ada penjualan bulan ini` |
| 7 | 5 Margin: `5 Produk Estimasi Margin Tertinggi Bulan Ini` (`Berdasarkan transaksi selesai bulan berjalan. HPP memakai cost movement, average cost, lalu harga beli jika diperlukan.`) + badge `Estimasi` | `top_estimated_margin_products_month[{… net_revenue:money, estimated_hpp:money, estimated_margin:money, estimated_margin_percent:money, cost_basis, counts}]` + `estimated_margin_status{basis:'operational_estimate', is_estimate:true, counters…, cost_basis}` | Hierarki-HPP 5-sumber + snapshot-sekali + status-kejujuran + urut-margin-turun; warning `Estimasi cost belum lengkap` (`{n} baris stok belum punya average cost atau harga beli.`, compact!) bila `missing>0`; baris `Omzet {x} \| Est. HPP {y}` + margin + persen; 3 kosong info/warning/info (teks persis di business-rules §2!) |
| 8 | Prioritas + Kritis: `Pesanan Prioritas` (`Pesanan {cabang} yang paling perlu ditindaklanjuti.` + `Lihat Semua`) · `Stok Kritis` (`Produk yang total stok aktifnya sudah berada di bawah atau sama dengan batas minimum.` + `Buka Stok`) | `priority_orders[{id_order, order_number, order_date, due_date, notes, status_label, status_group, party_name, priority_score, priority_reason:4-teks, age_days}]`, `critical_stock_items[{id_inventory_balance:MIN, id_product, product_name, available_qty:SUM, min_stock_qty:MAX, base_uom, updated_at:MAX}]` | Skor-5 (`100/80/60/40+umur-cap-30`, urut skor-turun/tempo-naik/order-naik) + daftar-5 (`(min−tersedia) DESC`); baris prioritas `{nomor, badge, pihak|Pihak terkait belum dipilih, Jatuh tempo {tgl|-}, alasan|catatan|-}` → `/orders/{id}`; baris kritis `{nama, Tersisa {n}, Minimum {n} {satuan} \| update terakhir {tgl}}` → `/stock/{idProduk}`; kosong → success `Tidak ada pesanan prioritas` / `Tidak ada stok kritis` |
| — | Diambil-tak-tampil | `order_summary.completed/cancelled`, `total_purchase/sales_transactions{count,amount:Number-mentah!}` | Kontrak API, tanpa UI (KI-134!); transaksi-total = completed sepanjang masa per jenis (hitung + nominal!) |
